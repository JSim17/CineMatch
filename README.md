# CineMatch
A Tinder-style movie matching app where users swipe to find movies they both like.

## Tech Stack
- Frontend: React
- Backend: Python (Flask/FastAPI)
- Database: (TBD)

## Setup

### Frontend
cd frontend
npm install
npm start

### Backend
cd backend
pip install -r requirements.txt
python app.py